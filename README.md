`jira-sprint-stats` is a utility tool which returns info about the current open sprint. It leverages the Jira API and JQL to fetch desired info.

It can be run with:
```
python3 ./main.py --api_token yourAPItoken --email your@email.com
```

